import 'package:flutter/material.dart';

class AppColors {
  // Base Neutrals - "Warm" whites and soft grays
  static const Color background = Color(0xFFF9FAFB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceMuted = Color(0xFFF3F4F6);
  static const Color border = Color(0xFFE5E7EB);

  // Text
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textTertiary = Color(0xFF9CA3AF);

  // Healthcare - Soft Teals/Sages
  static const Color healthcarePrimary = Color(0xFF0D9488);
  static const Color healthcareSurface = Color(0xFFF0FDFA);

  // Finance - Muted Indigos/Slates
  static const Color financePrimary = Color(0xFF4338CA);
  static const Color financeSurface = Color(0xFFEEF2FF);

  // Status Colors
  static const Color success = Color(0xFF059669);
  static const Color warning = Color(0xFFD97706);
  static const Color error = Color(0xFFDC2626);
}
